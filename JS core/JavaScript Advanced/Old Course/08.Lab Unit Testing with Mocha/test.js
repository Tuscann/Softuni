function solve(args) {
    let input = args.map(Number);

    for (let i = 1; i <= input; i++) {
        let row = '';
        row = row + ' ' + i;
        console.log(row);
        for ( j = 0; j < n; j++)
        {

        }

    }

}
solve(['2']);
solve(['2']);
solve(['-2']);
solve(['0']);
solve(['-3']);
